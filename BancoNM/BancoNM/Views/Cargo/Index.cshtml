@model IEnumerable<BancoNM.Cargos>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_compartido.cshtml";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Editar", new { id=item.idCargo }) |
            @Html.ActionLink("Details", "Detalles", new { id=item.idCargo }) |
            @Html.ActionLink("Delete", "Borrar", new { id=item.idCargo })
        </td>
    </tr>
}

</table>
