@model IEnumerable<BancoNM.Usuarios>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_compartido.cshtml";
}

    <h2>Inicio</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pass)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.acceso)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pass)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.acceso)
        </td>
        <td>
            @Html.ActionLink("Edit", "Editar", new { id = item.idUsuario }) |
            @Html.ActionLink("Details", "Detalles", new { id = item.idUsuario }) |
            @Html.ActionLink("Delete", "Borrar", new { id = item.idUsuario })
        </td>
    </tr>
}

</table>
